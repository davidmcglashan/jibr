arrays and vars
- setting from buckets: bucket array [arr] [id] puts keys in array
- setting from bucket names: bucket array [arr] names: makes an array of numbered bucket names (which are values from other queries)

scripts with params
- use variables or arrays 

for loops, e.g.
for key in keys
    jira key

record
- record update for "complex" fields
  - statuses (handle violation of workflow)

table or tlook
- version of look that outputs to a table, not JSON

comments
- record comments
- record comment [text]

vars
- using in open
- using in bucket match 'bucket match <var> <bucket id>'
- find other places to use

buckets
- bucket match <term> <var> - match exactly <term> can put the bucket id in <var>

issue
- issue <key> - displays a single record based on jbselect columns

buckets
- improvements to match
- bucket by 'field field field'

count
- having > 50
- count into bucket
- order by count asc/desc
- order by field asc/desc

payloads
- payload [key] - show just the payload for that record
- append with ?
- jira payload - open the payload's keys in a search: key in ()

things to do
- running filters
- managing filters
- parser to turn 'ins' into a list of strings where " has joined multiple words together, etc.
buckets
- ? where key in bucket
- payload bucket <id> - show the payload for just the bucket contents
- look bucket <id> - look's output but the contents are a bucket

help
- done as named text files rather than anything too structured

issue
- issue <key> - displays a single record based on jbselect columns

buckets
- bucket by 'field field field'
- bk <id> - <id> combine buckets (not numbered)

count
- order by count asc/desc
- order by field asc/desc

payloads
- save
- append
- export json
- export csv (excel)
- jira payload - open the payload's keys in a search: key in ()

arrays and vars
- setting from payload
- viewing
- combining
- using in where
- using in look
- using in open

keys command to pull keys out of payloads
- why?

